import 'package:flutter/material.dart';

class LastNewsScreen extends StatelessWidget {
  const LastNewsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    child : const Center(
    child: Text('Dernieres nouvelles'),
    ),
    );
  }
}
