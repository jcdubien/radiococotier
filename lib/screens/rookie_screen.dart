import 'package:flutter/material.dart';

class RookieScreen extends StatelessWidget {
  const RookieScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
